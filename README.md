# Koa API Example

An example API that uses Koa. Features:

- User registration
- Passport (local and JWT)
- ORM (typeorm)
- Router, CORS, logging

# Install

    yarn
    
# Start

Start the dev server (nodemon and ts-node):

    yarn start
